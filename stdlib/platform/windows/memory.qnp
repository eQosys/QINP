\\ QINP Standard Library
\\ Memory - Windows Specific

space std:
	pass



defer
space std:
	fn<void*> malloc(u64 size) nodiscard!:
		return null

	fn<void*> realloc(void* pBuff, u64 size) nodiscard!:
		return null

	fn<> free(void* pBuff)!:
		pass