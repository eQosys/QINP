\\ QINP Standard Library
\\ Linux Specific

import "../fileio.qnp"

space std:
	define __STDIN 0
	define __STDOUT 1
	define __STDERR 2

	i32 print(u8 const* str, u64 len)!:
		return fwrite((File*)__STDOUT, str, len)

	u8 getchar()!:
		u8 ch
		u64 result = fread((File*)__STDIN, &ch, 1)
		return ch