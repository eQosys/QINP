import "stdio.qnp"
import "system.qnp"

space std:
	void __assert(bool check, u8 const* msg, u8 const* file, u64 line):
		if !check:
			print("%:%: Assertion failed with message '%'!\n", file, line, msg)
			exit(1)