{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "group": "build",
            "type": "shell",

            "presentation": {
                "reveal": "always",
                "clear": true,
            },

            "options": {
                "cwd": "${workspaceFolder}",
            },
            
            "args": [ "Debug" ],

            "linux": {
                "command": "./build.sh",
            },
            "windows": {
                "command": "${cwd}\\build.bat",
            }
        }
    ]
}