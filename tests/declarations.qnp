var<i32> varDecl

var<i32> varDef = 10

var<i32[16]> arrDecl

var<i32*> ptrDecl

var<i32*> ptrDef = &varDecl

fn<> funcDecl(i32 arg)...

fn<bool> funcDef(u64 arg):
	return arg == 0

pack Data...

var<Data*> pData

pack Data:
	var<Data*> pSub
	var<i32> val

var<Data> data

enum Color:
	Red, Green,
	Blue